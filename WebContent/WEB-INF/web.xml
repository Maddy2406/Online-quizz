<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="http://xmlns.jcp.org/xml/ns/javaee"
    xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd"
    version="3.1">

    <display-name>OnlineQuizSystem</display-name>

    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!-- Servlet Mappings are handled by @WebServlet annotations in Java 1.7+, 
         but we ensure the container knows about them implicitly or we can define them explicitly here if needed.
         Since we used @WebServlet("/auth"), etc., explicit mapping is optional but good for references.
    -->
    
</web-app>
